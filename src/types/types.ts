export type Skill = {
  name: string;
  rank: number;
  level: number;
  xp: number;
};

export type Minigame = {
  name: string;
  rank: number;
  points: number;
};

export type Boss = {
  name: string;
  rank: number;
  kills: number;
};
